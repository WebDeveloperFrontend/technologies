# Базовый образ 
FROM python:alpine

# Рабочая директория (Путь к папке внутри образа).
WORKDIR /app

# Копирование наших файлов из на пк в папку образа.
COPY . .

# Запустит процесс исполняемого файла.
CMD [ "python", "main.py" ]